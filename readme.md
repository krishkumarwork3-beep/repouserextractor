# AWS S3 Event Triggering

AWS S3 Event triggering is a project that demonstrates how an S3 object
creation event can trigger an AWS Lambda function.

The Lambda function can process the uploaded object and send a notification
through Amazon SNS.